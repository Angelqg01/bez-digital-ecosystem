// Placeholder page: referenced by a lazy route in App.jsx but the original file
// was missing, which broke `vite build`. Minimal stub to keep the route valid.
export default function QualityControl() {
    return (
        <div style={{ padding: '2rem', textAlign: 'center', opacity: 0.7 }}>
            <h2>Quality Control</h2>
            <p>Módulo en preparación.</p>
        </div>
    );
}
