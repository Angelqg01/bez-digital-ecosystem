# BeZhas Aegis — Department Agents dependencies
# Añadir al requirements.txt principal de aegis/ o usar como overlay

# Core async
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx>=0.27.0

# Redis async
redis[asyncio]>=5.0.0

# Prometheus metrics (optional but recommended)
prometheus-client>=0.20.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# Ya deberían estar en aegis/requirements.txt:
# asyncpg>=0.29.0   (PostgreSQL async)
# python-dotenv>=1.0.0
