version: '3.8'
services:
  backend:
    volumes:
      - ./backend:/app
    command: pnpm exec nodemon server.js
    environment:
      - NODE_ENV=development
    ports:
      - "3001:3001"
  frontend:
    volumes:
      - ./frontend:/app
    command: pnpm run dev
    environment:
      - NODE_ENV=development
    ports:
      - "5173:5173"
